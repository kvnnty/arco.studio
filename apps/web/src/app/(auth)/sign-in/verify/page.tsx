import { MagicLinkSignInVerification } from "@/components/auth/magic-link-sign-in-verification";

export const metadata = { title: "Verify email" };

export default function SignInVerifyPage() {
  return <MagicLinkSignInVerification />;
}
