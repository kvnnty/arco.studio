import { SignupContinueForm } from "@/components/auth/signup-continue-form";

export const metadata = { title: "Continue sign up" };

export default function SignInContinuePage() {
  return <SignupContinueForm />;
}
